<?xml version="1.0" encoding="utf-8"?>
<resource>
    <paths>
        <!--<files-path/>代表的根目录： Context.getFilesDir()<cache-path/>代表的根目录: getCacheDir()<external-path/>代表的根目录: Environment.getExternalStorageDirectory()<external-files-path/>代表的根目录: Context.getExternalFilesDir(String) Context.getExternalFilesDir(null).<external-cache-path />代表的根目录: Context.getExternalCacheDir().<root-path />代表设备的根目录new File("/"); -->
        <!-- path=""代表根目录，也可以指定特定目录，name="camera_picture"是虚拟目录camera_picture -->
        <external-path
            name="file"
            path="." />
    </paths>
</resource>